# Legacy Express API (archived)

Superseded by **Next.js Server Actions** and **Route Handlers** in [`../apps/web`](../apps/web).

This MERN spike used MongoDB and JWT cookies. Do not extend. Use `npm run dev` from the repo root to run the modern app.
